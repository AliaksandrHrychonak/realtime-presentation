'use client';

import { PresentationList } from '@widgets/presentation-list';

import type { JSX } from 'react';

export const DashboardPresentationListPage = (): JSX.Element => {
    return <PresentationList />;
};
